# 2026-07-09

- 整理 `docs/BRD_v1.0.md` 与 `docs/SRS_v1.0.md` 为规范 Markdown：制表符表格转管道表格、API 章节的 `text` 标记符改为 http/json 代码围栏、补充目录与元信息属性表。EvoLib 图书馆 MVP 项目，B/S 架构，三角色（读者/馆员/管理员）。
- 新增 `docs/PROJECT_PLAN.md`：EvoLib 总体计划（V1.0）。技术栈锁定 Spring Boot 2.7.18 + JDK 8 + PostgreSQL + MyBatis-Plus + jjwt 0.11.x（因 JDK 1.8.0_401 不支持 Spring Boot 3.x）。16 个 WBS 任务共 44 点，单 Sprint 一周（07-09~07-16），5 个里程碑 M1~M5，5 项风险 R-01~R-05。
- SRS 更新：新增 REQ-00（用户登录认证，含 bcrypt、连续5次失败锁定15分钟、token 携带 readerId+role）；REQ-04 补充「归还后立即解冻，MVP 不设延迟解冻」；REQ-09 注明 V1.1 纳入 UI 配置；新增 6.2.0 登录接口。同步更新 PROJECT_PLAN.md：需求范围 REQ-00~REQ-11、T-03 改归属 REQ-00 并加锁逻辑（估点 3→4，总估点 45）、核心流程补登录、验收范围扩至 REQ-00。生成 `docs/WBS_v1.0.xlsx`（16 任务，含按模块配色、筛选、冻结窗格）。用 openpyxl（managed venv）生成；Windows 无 LibreOffice，recalc.py 不可用，SUM 改为硬编码值。
- 文档更新：BRD_v1.0.md 的 3.1 上方「系统交付物包含一套完整的Web操作界面…」句及 In-Scope 登录行已由用户/上游预先写入，确认已就位、未重复添加。USER_STORIES.md 补充前端交互：US-01/03/06 改为界面操作描述，全表新增「前端验收标准（GWT）」列（原有 US-02/04/05/07~11 补合理 GWT），并新增模块5 含 US-12（登录页）、US-13（左侧导航菜单）。
- SRS_v1.0.md 同步：磁盘文件已含第8章 UI 需求（P-01~P-10 页面清单 + UI-01~06 交互规则）且代码围栏已规范，未整体重写以免退化为粘贴版破损格式。本次修正：① 目录补第8章锚点链接；② 数据模型 5.3/5.4 的 `id` 约束 `PRIMARY KEY AUTO_INCREMENT` → `PRIMARY KEY 自增`（剔除 MySQL 语法，DDL 留给架构设计）。用户粘贴版末尾的「修正前后对比/最终结论」两段为评审注释，未写入 SRS 正文。
- 新增 `docs/ARCHITECTURE_DESIGN.md`（架构设计初稿）。基于 SRS v1.0 + BRD v1.0，以架构师视角设计。核心设计点：① 前端 HTML/CSS/JS 物理分离（禁止内联 script/style），CSS 变量主题模板（`theme.css` 白底 #FFF / 蓝按钮 #1890FF / 灰边框 #D9D9D9 → 一键换肤仅替换变量值）；② 后端按业务域垂直分包 `module/{auth,book,reader,borrow,admin,audit}`，Controller→Service→Mapper 三层隔离，为 Phase 3 微服务拆分预切面；③ 借书并发用 `UPDATE WHERE available_stock > 0` 行锁防超卖，不引入分布式锁；④ @Auditable 注解 + AOP 全量审计；⑤ 4 阶段架构演进路线图（单体→前后端分离部署→垂直拆服务→全分布式微服务），每阶段触发条件与迁移动作明确。含 PostgreSQL DDL（SERIAL/BIGSERIAL/JSONB）、SecurityConfig RBAC 路径矩阵、前端路由守卫、API 契约映射表、4 条 ADR 决策记录。
- **ARCHITECTURE_DESIGN.md 重写（v2）**：前端从原生 HTML/CSS/JS 切换为 **Vue 3 + Vite + Vue Router + Pinia** 全家桶。核心变更——① 新增 Vue 3 分层架构（Views → Components → Stores → Utils）；② `evolib-frontend/` 独立工程目录（Vite 构建输出到后端 `static/`）；③ Vue Router `beforeEach` 守卫实现登录拦截 + RBAC 角色校验（替代原 `router.js`）；④ Pinia `useAuthStore` 管理 token/role 状态（替代原 `auth.js` localStorage 操作）；⑤ Axios 拦截器自动注入 JWT + 处理 401/403（替代原 `fetch` 封装）；⑥ 自研 EvoButton/EvoInput/EvoTable/EvoModal 等 6 个 Vue 组件，全部通过 `var(--xxx)` 引用主题变量；⑦ 新增 BorrowView.vue 完整示例展示组件组合与 API 调用；⑧ CSS 变量体系新增 Element Plus SCSS 变量映射预留（Phase 2 对接方案）；⑨ ADR 从 4 条扩充为 5 条：新增 ADR-001（Vue 3 而非原生 JS）和 ADR-005（自研组件而非 Element Plus）。后端、DDL、API 契约、RBAC 矩阵、演进路线图保持不变。
