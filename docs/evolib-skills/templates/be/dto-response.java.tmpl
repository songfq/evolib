// 文件位置: module/{域}/dto/XxxResponse.java
// 使用方式: 复制此文件 → 替换 {{className}} / 字段列表

package com.evolib.module.{{moduleName}}.dto;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import java.time.LocalDate;

/**
 * {{接口名}} 响应 DTO
 * 
 * 字段来自 SRS 的「输出」列
 * 注意：不要把 Entity 直接暴露给前端！用 DTO 屏蔽数据库表结构变化。
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class {{className}}Response {

    // TODO: 按 SRS 的「输出」列补充字段
}
